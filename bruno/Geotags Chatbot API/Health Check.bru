meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/health
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Check if the service is running (no authentication required)
}
